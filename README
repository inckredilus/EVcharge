README file
This README was generated by the update_evcharge.sh script on 2025-12-04.

List of files created by the build process:
# update_evcharge.sh
# index.html
# src/main.jsx
# src/utils.js
# src/App.jsx
# src/components/Home.jsx
# src/components/StartForm.jsx
# src/components/CompleteFo rm.jsx
# src/components/ShowView.jsx
# src/styles.css

--------------------------------------------------------
Update script in ./tools/ finished. Project regenerated 2025-12-04
Files written and backups stored at: evcharge_backup_YYYYMMDD_HHMMSS

Next suggested steps:
  1) Inspect files and run a local build to verify:
       npm install
       npm run build

  2) Serve the dist/ locally to test (on PC):
       npx serve -s dist
     or
       python3 -m http.server 8000 -d dist

  3) If everything looks good, commit & push to git:
       git add .
       git commit -m "regen: full app regeneration — update BACK, SHOW, validation, layout and defaults"
       git push

Notes:
- The script does NOT touch node_modules or package.json.
- It backups replaced files into  so you can restore if needed.
--------------------------------------------------------